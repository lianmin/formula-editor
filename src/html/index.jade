doctype
html
    head
        meta(charset="utf-8")
        title test
        script(src="//g.alicdn.com/kissy/k/5.0.0/seed.js" data-config="{combine:false}")
        //- script(src="../js/lib/kissy/seed.js" data-config="{combine:false}")
        link(rel="stylesheet", href="../js/lib/kissy/css/base.css")
        link(rel="stylesheet", href="//gallery.kissyui.com/editor-plugins/doc/build/assets/editor-debug.css")

        script.
            require.config({
                packages: [
                    {
                        name: "edt",
                        tag: "20140918",
                        path: "../js/editor/",
                        charset: "utf-8"
                    },
                    {
                        name: "kg",
                        tag: "20141105",
                        path: 'http://g.alicdn.com/kg/',
                        charset: 'utf-8'
                    }
                ]
            });
    body
        h2 test
        div#editorContainer.editor
        script.

            require('node, editor, kg/editor-plugins/1.1.8/font-size', function($, Editor, FontSize){
                console.log($('h2'));


                var myEditor = new Editor({
                    focused : true,
                    attachForm : true,
                    render : '#editorContainer',
                    width : '500px',
                    height : '500px',
                    plugins : [
                        FontSize,
                    ]
                });

                myEditor.render();

                myEditor.on('blur', function () {
                    console.log(myEditor.getSelectedHtml());
                });
            });

